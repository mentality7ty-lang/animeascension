import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import SpecializationClient from './specialization-client';export default async function Page(){const db=await createClient();const {data:x}=await db.auth.getClaims();const userId=x?.claims?.sub;if(!userId)redirect('/login');const {data:c}=await db.from('characters').select('name,power,strength,speed,endurance,focus,rank').eq('user_id',userId).maybeSingle();if(!c||!c.rank?.toLowerCase().includes('vanguard'))redirect('/village');return <SpecializationClient userId={userId} initialCharacter={c}/>}
