import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import Client from './control-client';export default async function Page(){const db=await createClient();const {data:x}=await db.auth.getClaims();const id=x?.claims?.sub;if(!id)redirect('/login');const {data:c}=await db.from('characters').select('name,power,strength,speed,endurance,focus,rank,world').eq('user_id',id).maybeSingle();if(!c||c.world!=='cursed'||!c.rank?.toLowerCase().includes('cursed awakened'))redirect('/cursed-metropolis');return <Client userId={id} character={c}/>}
