import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import Client from './resonance-client';export default async function Page(){const db=await createClient();const {data:x}=await db.auth.getClaims();const id=x?.claims?.sub;if(!id)redirect('/login');const {data:c}=await db.from('characters').select('name,power,focus,rank,world').eq('user_id',id).maybeSingle();if(!c||!c.rank?.toLowerCase().includes('borderbreaker'))redirect('/village');return <Client userId={id} initialCharacter={c}/>}
