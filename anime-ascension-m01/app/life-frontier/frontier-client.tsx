'use client';import Link from 'next/link';import {motion} from 'framer-motion';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';type C={name:string;power:number;strength:number;speed:number;endurance:number;focus:number;rank:string;world:string};export default function Client({userId,character:c}:{userId:string;character:C}){const db=createClient(),r=c.rank.toLowerCase(),arrived=r.includes('aura sensed'),initiated=r.includes('aura initiated'),fundamentals=r.includes('aura fundamentals'),tested=r.includes('riven tested'),valley=r.includes('valley marked'),relay=r.includes('relay decoded'),hunter=r.includes('relay hunter');const [phase,setPhase]=useState<'gate'|'arrival'|'sense'|'done'>(arrived?'done':c.world==='frontier'?'arrival':'gate'),[signal,setSignal]=useState(0),[busy,setBusy]=useState(false),[msg,setMsg]=useState(hunter?'CUSTODIAN SIGNAL · INCOMING':relay?'CROSS-WORLD RELAY · DECODED':valley?'NULL MONOLITH FOUND':tested?'RIVEN TEST · PASSED':'LIFE AURA');async function cross(){await db.from('characters').update({world:'frontier'}).eq('user_id',userId);setPhase('arrival')}function listen(){if(busy)return;setBusy(true);const s=Math.min(100,signal+25);setSignal(s);setTimeout(()=>{setBusy(false);if(s>=100)void finish()},300)}async function finish(){await db.from('characters').update({power:c.power+25,focus:c.focus+2,rank:`${c.rank} · Aura Sensed`}).eq('user_id',userId);setPhase('done')}return <main className="frontier-shell"><div className="frontier-sky"/><div className="frontier-mountains"/><div className="frontier-forest"/><header><span>WORLD 03 // LIFE FRONTIER</span><strong>{msg}</strong></header>{phase==='gate'&&<section className="frontier-card"><h1>No city. No village. Just life.</h1><button onClick={cross}>CROSS INTO WORLD 03</button></section>}{phase==='arrival'&&<section className="arrival-three"><motion.div className="frontier-player" initial={{opacity:0}} animate={{opacity:1}}><i/></motion.div><article><h1>The forest is watching.</h1><button onClick={()=>setPhase('sense')}>SENSE THE FOREST</button></article></section>}{phase==='sense'&&<section className="sense-card"><h1>Notice life.</h1><div className="sense-orb"><b>{signal}%</b></div><button disabled={busy} onClick={listen}>LISTEN</button></section>}{phase==='done'&&<section className="frontier-card complete"><span>{hunter?'NETWORK RESPONSE':relay?'NETWORK DISCOVERED':'LIFE AURA'}</span><h1>{hunter?'IT FOUND YOU.':relay?'THE GATES WERE ENGINEERED.':'LIFE AURA'}</h1><p>{hunter?'Three relay echoes restored 75% of World 04. Your scans triggered a response from the Network Custodian.':relay?'World 04 exists, but its address is fragmented.':'Your World 03 progression continues.'}</p><div className="energy-stack"><b>CHAKRA · ACTIVE</b><b>CURSED ENERGY · ACTIVE</b><b>LIFE AURA · {fundamentals?'COMBAT READY':'SENSED'}</b></div>{!initiated?<Link href="/life-frontier/first-encounter">FIRST ENCOUNTER →</Link>:!fundamentals?<Link href="/life-frontier/fundamentals">AURA FUNDAMENTALS →</Link>:!tested?<Link href="/life-frontier/riven-spar">CHALLENGE RIVEN →</Link>:!valley?<Link href="/life-frontier/silent-valley">ENTER SILENT VALLEY →</Link>:!relay?<Link href="/life-frontier/null-monolith">INVESTIGATE NULL MONOLITH →</Link>:!hunter?<Link href="/life-frontier/relay-hunt">BEGIN RELAY HUNT →</Link>:<><p className="warning">NETWORK CUSTODIAN · UNAUTHORIZED ASCENDER DETECTED</p><button disabled>CUSTODIAN SIGNAL · NEXT MILESTONE</button></>}<Link href="/village">RETURN THROUGH GATE</Link></section>}</main>}
