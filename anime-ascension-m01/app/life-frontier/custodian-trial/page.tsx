import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import CustodianTrialClient from './trial-client';export default async function Page(){const db=await createClient();const {data:x}=await db.auth.getClaims();const id=x?.claims?.sub;if(!id)redirect('/login');const {data:c}=await db.from('characters').select('name,power,strength,speed,endurance,focus,rank,world').eq('user_id',id).maybeSingle();if(!c||c.world!=='frontier'||!c.rank?.toLowerCase().includes('custodian ping'))redirect('/life-frontier');return <CustodianTrialClient userId={id} character={c}/>}
