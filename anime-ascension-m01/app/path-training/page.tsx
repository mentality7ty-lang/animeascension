import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import PathTrainingClient from './path-training-client';export default async function Page(){const db=await createClient();const {data:x}=await db.auth.getClaims();const id=x?.claims?.sub;if(!id)redirect('/login');const {data:c}=await db.from('characters').select('name,power,strength,speed,endurance,focus,rank').eq('user_id',id).maybeSingle();if(!c||!c.rank?.toLowerCase().includes('path:'))redirect('/village');return <PathTrainingClient userId={id} initialCharacter={c}/>}
